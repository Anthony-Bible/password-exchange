apiVersion: v1
kind: Service
metadata:
  name: database-service 
  annotations:
  namespace: default
spec:
  selector:
    app: database
    phase: PROD
  ports:
    - name: grpc 
      port: 50051 
      targetPort: 50051

