apiVersion: v1
kind: Service
metadata:
  name: database-service 
  annotations:
  namespace: default
spec:
  selector:
    app: database 
  ports:
    - name: http
      port: 50051 
      targetPort: 50051

