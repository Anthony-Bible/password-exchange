apiVersion: v1
kind: Service
metadata:
  name: encryption-dev-service 
  annotations:
    # external-dns.alpha.kubernetes.io/hostname: anthony.bible
  namespace: default
spec:
  selector:
    app: encryption 
    phase: DEV
  ports:
    - name: grpc 
      port: 50051 
      targetPort: 50051

